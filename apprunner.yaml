version: 1.0
runtime: python3
build:
  commands:
    build:
      - python -m pip install --upgrade pip
      - pip install -r requirements.txt
run:
  command: python app.py
  network:
    port: 8080
    env: PORT
  env:
    - name: PORT
      value: "8080"
